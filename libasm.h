#ifndef LIBASM_H
# define LIBASM_H

# include <stdio.h>
# include <stddef.h>
# include <unistd.h>
# include <string.h>

size_t  ft_strlen(const char* s);
char*   ft_strcpy(char* restrict dst, const char* restrict src);

#endif
